# Copy to .env and fill in. Never commit .env (gitignored).

# SQLite in local dev (ADR-002); PostgreSQL in production.
DATABASE_URL="file:./dev.db"

# NextAuth v5 session secret — generate: npx auth secret  (or: openssl rand -base64 32)
AUTH_SECRET="change-me"
